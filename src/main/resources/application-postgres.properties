spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/flip_iq}
spring.datasource.username=${DATABASE_USERNAME:flip_iq}
spring.datasource.password=${DATABASE_PASSWORD:flip_iq}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: flip_iq
      POSTGRES_USER: flip_iq
      POSTGRES_PASSWORD: flip_iq
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
